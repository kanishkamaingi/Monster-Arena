<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>profile</title>
    <link rel="stylesheet" href="/css/main.css" />
    
  </head>
  <body class=" text-white min-h-screen flex flex-col">
      <%- include('partials/navbar', {page: '/profile'}) %>
    <header class=" py-4 shadow-md">
      <div class="container mx-auto px-4">
        <h1 class="text-3xl font-bold text-center">Welcome, <%= userName %></h1>
      </div>
    </header>
  
    <main class="container mx-auto px-4 py-6">
      <h2 class="text-center text-2xl font-bold mb-4">Your Created Monsters</h2>
  
      <% if (userSets.length > 0) { %>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          <% userSets.forEach(set => { %>
            <div class="bg-gray-800 rounded-lg shadow-md p-4">
              <img src="<%= set.img_url || '/default-image.png' %>" alt="<%= set.name %>" class="w-full h-48 object-cover rounded-md mb-4">
              <h3 class="text-xl font-semibold"><%= set.name %></h3>
              <p class="text-gray-400">Year: <%= set.year %></p>
              <p class="text-gray-400">Power Level: <%= set.power_level %></p>
              <p class="text-gray-400">Theme ID: <%= set.theme_id %></p>
              <a href="/monsters/collection/<%= set.monster_id %>" class="block mt-4 text-blue-500 hover:underline">View Details</a>
            </div>
          <% }); %>
        </div>
      <% } else { %>
        <p class="text-gray-400 text-center">You haven't created any sets yet.</p>
      <% } %>
    </main>
  
    <footer class=" py-4 text-center text-sm text-gray-400 mt-auto">
      &copy; <%= new Date().getFullYear() %> Monster Arena. All rights reserved.
    </footer>
  </body>
</html>